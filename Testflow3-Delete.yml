info:
  name: Testflow3-Delete
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/posts/{{e2ePostId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("DELETE: Status 200", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
