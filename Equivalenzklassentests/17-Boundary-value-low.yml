info:
  name: 17-Boundary-value-low
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/{{minId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("First valid User-ID (Grenzwert)", function() {
          expect(res.getStatus()).to.equal(200);
          const body = res.getBody();
          expect(body.id).to.equal(1);
          expect(body).to.have.property("name");
          expect(body).to.have.property("email");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
