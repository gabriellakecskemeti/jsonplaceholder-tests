info:
  name: 18-Boundary-value-max
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/{{maxId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Last valid User-ID (Grenzwert)", function() {
          expect(res.getStatus()).to.equal(200);
          const body = res.getBody();
          expect(body.id).to.equal(10);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
