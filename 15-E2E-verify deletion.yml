info:
  name: 15-E2E-verify deletion
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/posts/{{e2ePostId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status ist 404 NOT FOUND", function() {
          const status = res.getStatus();
          expect(status).to.be.oneOf([200, 404]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
