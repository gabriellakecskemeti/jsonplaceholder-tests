info:
  name: 13-E2E-get read
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/posts/{{e2ePostId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        //jsonplaceholder do not save new post
        test("GET: Status 404", function() {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
