info:
  name: 10-GET-State verification baseline
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/posts/1"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // Originalwerte speichern
        const body = res.getBody();
        bru.setVar("originalTitle", body.title);
        bru.setVar("originalBody", body.body);
        bru.setVar("originalUserId", body.userId);
        console.log("Original-Titel: " + body.title);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
