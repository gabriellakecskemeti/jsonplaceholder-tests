info:
  name: "08-POST-Post-Respond Script "
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/posts"
  body:
    type: json
    data: '{ "title": "Variable Test", "body": "Inhalt", "userId": 1 }'
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        //Extract value from response and save as variable 
        const body = res.getBody();
        const newId = body.id;
        bru.setVar("createdPostId", newId);
        console.log("Gespeicherte ID: " + newId);
    - type: tests
      code: |-
        test("Status is 201", function() {
          expect(res.getStatus()).to.equal(201);
        });

        test("ID has been assigned", function() {
          const body = res.getBody();
          expect(body).to.have.property("id");
          expect(body.id).to.be.a("number");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
