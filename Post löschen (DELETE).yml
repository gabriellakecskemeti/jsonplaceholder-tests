info:
  name: Post löschen (DELETE)
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/posts/1"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status ist 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });


        test("Body leer", function() {
          const body = res.getBody();
          expect(Object.keys(body)).to.have.length(0);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
